Skill sources live here as folders: <skill-name>/SKILL.md (frontmatter: name,
description) + optional supporting files. The harness CLI copies them verbatim
into each enabled tool's skills dir (e.g. .claude/skills/) — edit here, then
run `harness doctor --fix` to propagate.
